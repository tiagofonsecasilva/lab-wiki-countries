import React from "react";
import { NavLink } from "react-router-dom";

function NavBar() {
    return (
        <nav>
        <h4>LAB - WikiCountries</h4>
        </nav>
    );
}

export default NavBar;